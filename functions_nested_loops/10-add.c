#include "main.h"

/**
 * add - function that adds two integers and returns the result.
 * @x: The first number to be computed.
 * @y: The second number to be computed.
 *
 * Return: Add of the two integers X,Y.
 */
int add(int x, int y)
{
	return (x + y);
}
