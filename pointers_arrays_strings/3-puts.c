#include "main.h"

/**
 * _puts - prints a string, followed by a new line, to stdout.
 * @str: a string
 *
 * Return: void
 */

void _puts(char *str)
{
	for (str[i] != '\0'; ++i)
		_putchar(str[i]);

	_putchar('\n');
}
